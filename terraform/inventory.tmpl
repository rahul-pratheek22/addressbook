all:
  hosts:
    remoteserver1:
      ansible_host: ${remote_ips[0]}
      ansible_user: ec2-user
      ansible_ssh_private_key_file: /var/lib/jenkins/workspace/addressbook-pipeline/terraform/centoskey.pem
    remoteserver2:
      ansible_host: ${remote_ips[1]}
      ansible_user: ec2-user
      ansible_ssh_private_key_file: /var/lib/jenkins/workspace/addressbook-pipeline/terraform/centoskey.pem
  
  children:
    webservers:
      hosts:
        remoteserver1:
        remoteserver2:
    demoall:
      children:
        webservers: